package de.rub.bi.inf.openbimrl.functions.geometry;

import de.rub.bi.inf.openbimrl.NodeProxy;
import de.rub.bi.inf.openbimrl.functions.AbstractFunction;

/**
 * Loads a representation item newly created into the model context, and therefore into the viewer.
 *
 * @author Marcel Stepien (reworked by Florian Becker)
 */
public class LoadRepresentationItemGeometry extends AbstractFunction {

    public LoadRepresentationItemGeometry(NodeProxy nodeProxy) {
        super(nodeProxy);
    }

    @Override
    public void execute() {
        // TODO redo
    }
}
