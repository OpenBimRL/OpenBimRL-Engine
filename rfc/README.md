# OpenBimRL Engine RFCs

Request for Comments documents describe proposed engine features and architectural decisions before implementation.

Pushes to `master` or `dev` that change files under `rfc/` automatically create or update a matching GitHub issue (labels `rfc`, `rfc-<n>`). Run the **Sync RFC Issues** workflow manually to resync all RFCs.

| RFC | Title | Status |
|-----|-------|--------|
| [RFC-1](rfc-1-custom-script-functions.md) | Custom Script Functions | Draft |
